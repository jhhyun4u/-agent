# layout
Cohesion: 1.00 | Nodes: 1

## Key Nodes
- **layout** (C:\project\tenopa proposer\frontend\app\editor\layout.tsx) -- 0 connections

## Internal Relationships

## Cross-Community Connections

## Context
이 커뮤니티는 layout를 중심으로 related 관계로 연결되어 있다. 주요 소스 파일은 layout.tsx이다.

### Key Facts
- /** * 편집 전용 레이아웃 — AppSidebar 없이 전체화면 */ export default function EditorLayout({ children, }: { children: React.ReactNode; }) { return <>{children}</>; }
