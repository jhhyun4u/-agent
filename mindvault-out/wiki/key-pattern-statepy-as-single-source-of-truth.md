# Key pattern: state.py as single source of truth
Cohesion: 1.00 | Nodes: 1

## Key Nodes
- **Key pattern: state.py as single source of truth** (C:\project\tenopa proposer\-agent-master\.claude\agent-memory\bkit-pdca-iterator\project_step8a_act1.md) -- 0 connections

## Internal Relationships

## Cross-Community Connections

## Context
이 커뮤니티는 Key pattern: state.py as single source of truth를 중심으로 related 관계로 연결되어 있다. 주요 소스 파일은 project_step8a_act1.md이다.

### Key Facts
- **How to apply:** When adding new STEP 8 models, define them in `state.py` (Phase 2 section), add state fields to `ProposalState`, and re-export from `step8_schemas.py` if needed by external consumers.
